{
  "name": "voicedrop-signaling",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.16.15",
    "@cloudflare/workers-types": "^4.20241205.0",
    "@noble/curves": "^2.2.0",
    "typescript": "^6.0.3",
    "vitest": "~4.1.8",
    "wrangler": "^4.105.0"
  }
}
