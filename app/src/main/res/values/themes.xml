<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.VoiceDrop" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Pinned brand palette — overrides any device-side dynamic color. -->
        <item name="colorPrimary">@color/md_primary</item>
        <item name="colorOnPrimary">@color/md_on_primary</item>
        <item name="colorPrimaryContainer">@color/md_primary_container</item>
        <item name="colorOnPrimaryContainer">@color/md_on_primary_container</item>
        <item name="colorSecondary">@color/md_secondary</item>
        <item name="colorOnSecondary">@color/md_on_secondary</item>
        <item name="colorSecondaryContainer">@color/md_secondary_container</item>
        <item name="colorOnSecondaryContainer">@color/md_on_secondary_container</item>
        <item name="colorTertiary">@color/md_tertiary</item>
        <item name="colorOnTertiary">@color/md_on_tertiary</item>
        <item name="colorSurface">@color/md_surface</item>
        <item name="colorSurfaceVariant">@color/md_surface_variant</item>
        <item name="colorOnSurface">@color/md_on_surface</item>
        <item name="colorOnSurfaceVariant">@color/md_on_surface_variant</item>
        <item name="colorOutline">@color/md_outline</item>
        <item name="colorError">@color/md_error</item>
        <item name="colorErrorContainer">@color/md_error_container</item>

        <!-- Custom warning tokens for the AEAD banner. -->
        <item name="colorWarningContainer">@color/md_warning_container</item>
        <item name="colorOnWarningContainer">@color/md_on_warning_container</item>

        <!-- Manrope is the default font family for every default-styled view. -->
        <item name="android:fontFamily">@font/manrope</item>
        <item name="fontFamily">@font/manrope</item>

        <!-- Phase C removes this opt-out. Kept here so commit 1 is independently sane
             (no edge-to-edge insets handler yet). -->
        <item name="android:windowOptOutEdgeToEdgeEnforcement" tools:targetApi="35">true</item>
    </style>

    <style name="Theme.VoiceDrop.Translucent" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <!-- Toolbar title — slightly tighter and heavier than M3 default. -->
    <style name="TextAppearance.VoiceDrop.Wordmark" parent="TextAppearance.Material3.TitleLarge">
        <item name="android:fontFamily">@font/manrope</item>
        <item name="fontFamily">@font/manrope</item>
        <item name="android:textSize">20sp</item>
        <item name="android:letterSpacing">-0.015</item>
        <item name="android:textFontWeight">600</item>
    </style>

    <!-- Settings section header — uppercase, tracked, tertiary-colored. -->
    <style name="TextAppearance.VoiceDrop.SectionLabel" parent="TextAppearance.Material3.LabelMedium">
        <item name="android:fontFamily">@font/manrope</item>
        <item name="fontFamily">@font/manrope</item>
        <item name="android:textSize">11sp</item>
        <item name="android:letterSpacing">0.08</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:textFontWeight">600</item>
        <item name="android:textColor">?attr/colorTertiary</item>
    </style>

</resources>
